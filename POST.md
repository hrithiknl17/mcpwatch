## This file is unwritten

`POST.md` is the single source for the body of the page — the build renders it
verbatim below the table, and duplicates nothing into the page code.

Replace this file with the writeup. The build will fail if it is deleted or
emptied.
